{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "set -eux; \
      cd pleasantcovedesign/server && npm ci && npm run build; \
      cd ../admin-ui && npm ci && VITE_API_URL=/api VITE_WS_URL= npm run build; \
      mkdir -p ../server/dist/public/admin; \
      cp -R dist/* ../server/dist/public/admin/; \
      ls -la ../server/dist/public/admin"
  },
  "deploy": {
    "startCommand": "node pleasantcovedesign/server/dist/index.js",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
